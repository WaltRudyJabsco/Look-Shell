# LOOK 0.4.0

The unified command remains `lk`.

0.4 adds interactive selection/actions after incremental filtering:

- Enter a directory to browse it inside LOOK.
- Enter a file to open it with the operating system default.
- `e` edits, `y` copies the full path, and `p` prints the full path.

All existing short commands and pager keys remain unchanged.
