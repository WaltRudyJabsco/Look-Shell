# LOOK 0.6.11

Surgical shortcut-routing fix.

- `l`, `ll`, `ld`, `lf`, `lt`, `lr`, and `lz` all force interactive LOOK mode.
- Explicit `lk ...` commands keep their existing quick print-and-return behavior when the output fits.
- No renderer, Ollama, memory, installer, or filesystem behavior changes.
